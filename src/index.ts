import { Command } from "commander";

const program = new Command();

program
  .name("glideapi")
  .description("CLI to scaffold projects and generate modules for backend development")
  .version("1.1.0");
